/**
 * Public surface of the files module: the page the app mounts and the reducer
 * the store mounts. Every other file in this folder is private to the feature.
 */
export { FilesPage } from './pages/FilesPage.jsx'
export { filesReducer } from './files.slice.js'
